# A transitive Cargo dep (indexmap 2.14) needs the edition2024 feature, stabilized
# in Rust 1.85. Without this pin the solver defaults to rust 1.77.2 on win-64 and
# `cargo metadata` fails. Pinned rather than a floor so all platforms match.
rust_compiler_version:
  - "1.87"
